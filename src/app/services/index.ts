import { AuthService } from './auth.service';
import { ConfigService, SiteTheme } from './config.service';

export const SERVICES = [AuthService, ConfigService];

export {
  AuthService,
  ConfigService,
  SiteTheme
};
